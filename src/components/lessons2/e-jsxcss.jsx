// Internal CSS

import React from "react";

const Internal = () => {
  const styles = {
    color: "blue",
    backgroundColor: "yellow",
  };
  const age = 1000000;
  return <h1 style={styles}>{age}</h1>;
};

export default Internal;
